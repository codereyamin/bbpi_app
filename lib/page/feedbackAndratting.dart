// ignore_for_file: file_names

import 'package:flutter/material.dart';

class FeedbackAndRatting extends StatelessWidget {
  const FeedbackAndRatting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Scaffold(
        body: Center(child: Text('add will be next update')),
      ),
    );
  }
}
